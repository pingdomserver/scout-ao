---
version: 1

schedule:
  type: cron
  interval: "0 * * * * *"

workflow:
  collect:
    metrics:
      /psm/*: {}
      /system/*: {}
    tags:
      /:
        roles: <%= roles %>
        environment: <%= environment %>
        hostname: <%= hostname %>
    publish:
      - plugin_name: publisher-appoptics
        config:
          period: 60
          floor_seconds: 60
